apiVersion: v1
kind: Service
metadata:
  name: todolist-service
  labels:
    app: todolist
spec:
  selector:
    app: todolist
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
